[project]
name = "devfoundry-backend"
version = "0.1.0"
requires-python = ">=3.11"

[tool.pytest.ini_options]
anyio_mode = "auto"
testpaths = ["tests"]

[dependency-groups]
dev = ["pytest>=8", "anyio>=4", "httpx>=0.27"]
